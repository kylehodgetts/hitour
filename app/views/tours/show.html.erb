<%= react_component('SingleTour',
										showUrl: tour_path + '.json',
									  points_url: points_path + '.json',
									  new_tour_point_url: create_tour_point_path,
										update_tour_url: update_tour_url(@tour.id),
									  tour_id: @tour.id,
										audiences: @audiences) %>
