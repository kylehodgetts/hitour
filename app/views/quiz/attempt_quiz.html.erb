<%= react_component('PublicQuiz', quizData: @quiz_data,
                                  tourId: @tour.id,
                                  tour: @tour,
                                  feedbackPostUrl: create_feedback_path) %>
